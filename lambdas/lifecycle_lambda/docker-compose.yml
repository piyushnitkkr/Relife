version: '3.8'

# For local testing of the Lambda function
services:
  lifecycle-lambda:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9002:8080"
    environment:
      - AWS_REGION=us-east-1
      - DYNAMO_TABLE_NAME=relife-user-credits
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-}
